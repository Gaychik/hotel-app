# Контекст проекта Hotel App

## Общее описание

Это веб-приложение для бронирования отелей "Hotel California". Проект представляет собой современный сайт отеля с адаптивным дизайном, реализованный на стеке Next.js/React.

## Цель проекта

Создать удобный и современный интерфейс для потенциальных гостей отеля, позволяющий:
- Просматривать информацию об отеле
- Осуществлять поиск номеров
- Авторизоваться в системе
- Управлять бронированиями
- Просматривать профиль пользователя
- Выбирать даты пребывания через умный календарь

## Технический стек

### Основные технологии
- **Next.js 14.2.3** - React фреймворк с App Router
- **React 18.3.1** - Библиотека для построения пользовательского интерфейса
- **TypeScript 5** - Строгая типизация для JavaScript
- **Tailwind CSS 3.4.3** - Utility-first CSS фреймворк

### Вспомогательные библиотеки
- **react-transition-group 4.4.5** - Анимации и переходы
- **@types/react-transition-group 4.4.10** - Типы для react-transition-group
- **@types/node 20** - Типы для Node.js
- **@types/react 18** - Типы для React
- **@types/react-dom 18** - Типы для React DOM
- **@heroicons/react** - Иконки для интерфейса
- **date-fns** - Работа с датами
- **react-day-picker** - Компонент выбора дат

### Инструменты разработки
- **ESLint 8** - Линтинг кода
- **eslint-config-next 14.2.3** - Конфигурация ESLint для Next.js
- **PostCSS 8** - Обработка CSS
- **Autoprefixer 10.4.19** - Автопрефиксер для CSS

## Архитектура приложения

### Структура директорий

```
```
.
├── app/                    # Next.js App Router
│   ├── api/
│   │   └── auth/[...nextauth]/
│   │       └── route.ts      # NextAuth.js API route
│   ├── booking/
│   │   ├── success/
│   │   │   └── page.tsx      # Success page after booking
│   │   └── page.tsx          # Booking page
│   ├── calendar/
│   │   └── page.tsx          # Calendar page
│   ├── login/
│   │   └── page.tsx          # Login page
│   ├── profile/
│   │   └── page.tsx          # User profile page
│   ├── rooms/
│   │   ├── [id]/
│   │   │   └── page.tsx      # Dynamic room detail page
│   │   └── page.tsx          # Rooms catalog page
│   ├── favicon.ico
│   ├── globals.css           # Global styles with Tailwind directives
│   ├── layout.tsx            # Root layout with font settings
│   └── page.tsx              # Home page (MainScreen)
├── components/               # React components
│   ├── calendar/
│   │   ├── BookingDetailsPanel.tsx
│   │   ├── CalendarDayContent.tsx
│   │   
│   ├── layout/
│   │   ├── Header.tsx        # Header with navigation
│   │   ├── HeroSection.tsx   # Hero section (banner)
│   │   └── InfoSection.tsx   # Info section
│   ├── profile/
│   │   ├── CurrentBookingCard.tsx
│   │   ├── HistoryBookingCard.tsx
│   │   └── ProfileSettings.tsx
│   ├── ui/
            BackButton.tsx# Кнопка назад в виде стрелочки
│   │   ├── BookingWidget.tsx # Booking widget
│   │   ├── HeroSearchWidget.tsx
│   │   ├── Icons.tsx         # SVG icons
│   │   ├── ImageGallery.tsx  # Image gallery
│   │   ├── InfoCard.tsx      # Info cards
│   │   ├── RoomDetails.tsx   # Room details
│   │   ├── RoomPolicies.tsx  # Room policies
│   │   ├── SearchButton.tsx  # Search button
│   │   └── SearchPill.tsx    # Search pill component
│   ├── BookingForm.tsx       # Booking form
│   ├── LoginScreen.tsx       # Login screen with SMS auth
│   ├── MainScreen.tsx        # Main screen (entry point)
│   └── RoomCard.tsx          # Hotel room card with image slider
├── data/
│   └── rooms.ts              # Room data and types
├── lib/
│   └── data.ts               # Helper functions for data handling
├── project-context/
│   ├── context.md            # Project context (this file)
│   └── rules.md              # Project rules and standards
├── providers/
│   └── NextAuthProvider.tsx  # NextAuth provider wrapper
├── public/
│   ├── images/
│   │   ├── deluxe-1.avif
│   │   └── deluxe-2.avif
│   ├── file.svg
│   ├── globe.svg
│   ├── hero1.png
│   ├── hero2.jpg
│   ├── hero3.jpg
│   ├── hero4.jpg
│   ├── next.svg
│   ├── vercel.svg
│   └── window.svg
├── .env.local
├── .gitignore
├── README.md
├── eslint.config.mjs
├── next-env.d.ts
├── next.config.js
├── package-lock.json
├── package.json
├── postcss.config.js
├── tailwind.config.ts
├── tsconfig.json
├── tsconfig.tsbuildinfo
└── types.ts                  # Global TypeScript types
```
```

### Основные компоненты

### MainScreen
Главный компонент приложения, который объединяет все основные секции:
- Header (шапка)
- HeroSection (герой-баннер)
- InfoSection (информация об отеле)

### Header
Адаптивная шапка сайта с:
- Логотипом "HC" / "Hotel California"
- Мобильным меню (гамбургер)
- Иконками навигации:
  - Избранное (HeartIcon)
  - Бронирования (CalendarIcon)
  - Профиль пользователя (UserIcon)

### HeroSection
Герой-секция с фоновым изображением и:
- Заголовком "Отдых в Hotel California"
- Подзаголовком "Найдите идеальное место для вашего отдыха"
- Кнопками поиска (место, даты, гости)

### InfoSection
Секция с информацией об отеле, содержит карточки с:
- Удобствами отеля
- Сервисами
- Преимуществами

### RoomsPage (/rooms)
Страница каталога номеров отеля с:
- Заголовком "Доступные номера"
- Сеткой карточек RoomCard (3 колонки на десктопе)
- Отображением всех доступных номеров из roomsData
- Адаптивной версткой (1 колонка на мобильных, 2 на планшетах)

### RoomCard
Карточка номера отеля с:
- Слайдером изображений (автосмена при наведении)
- Описанием номера
- Списком удобств
- Ценой за ночь
- Кнопками "Подробнее" и "Выбрать"

### SmartCalendar
Будущий умный календарь, самая проблемная зона

### BookingForm
Форма бронирования с:
- Полями для ввода данных клиента
- Выбором количества гостей
- Подтверждением бронирования
- Валидацией введенных данных

### Profile Components
Компоненты профиля пользователя включают:
- ProfileSettings - настройки профиля
- CurrentBookingCard - карточка текущего бронирования
- HistoryBookingCard - карточка истории бронирования

### LoginScreen
Экран авторизации с:
- Вводом номера телефона
- Подтверждением по SMS-коду
- Возможностью входа через социальные сети (Telegram, VK, Google)

## Дизайн и стилизация

### Шрифты
Используются 4 шрифта от Google Fonts:
1. **Inter** - основной текст (переменная --font-inter)
2. **Karantina** - логотип и заголовки (--font-karantina)
3. **Istok Web** - подзаголовки (--font-istok-web)
4. **Source Serif 4** - акцентный текст (--font-source-serif-pro)

### Цветовая палитра
- Основной фон: белый/светло-серый
- Акцентные цвета: синие оттенки
- Текст: черный/темно-серый

### Адаптивность
- Мобильная версия: до 768px
- Планшетная версия: 768px - 1024px
- Десктопная версия: от 1024px

## Текущее состояние проекта

### Реализовано
✅ Главная страница с Hero секцией
✅ Адаптивный Header с мобильным меню
✅ Info секция с карточками
✅ Система шрифтов
✅ Базовая маршрутизация (главная, логин, профиль, rooms)
✅ UI компоненты (иконки, кнопки)
✅ Карточки номеров отеля (RoomCard) с автослайдером изображений
✅ Страница каталога номеров (/rooms)
✅ Динамические страницы номеров (/rooms/[id])
✅ Система данных о номерах (rooms.ts)
✅ Экран авторизации с SMS-подтверждением и социальными сетями
✅ Глобальные стили с правильной конфигурацией Tailwind CSS
✅ Анимации переходов между слайдами
✅ Система типизации TypeScript для всех компонентов
✅ Страница календаря (/calendar) с возможностью выбора дат
✅ Страница бронирования (/booking) и подтверждения (/booking/success)
✅ Компоненты календаря (SmartCalendar, CalendarDayContent, BookingDetailsPanel)
✅ Компоненты профиля (CurrentBookingCard, HistoryBookingCard, ProfileSettings)
✅ Форма бронирования (BookingForm)
✅ Компоненты галереи и деталей номера (ImageGallery, RoomDetails, RoomPolicies)

### В разработке
🚧 Система избранного
🚧 Интеграция с backend/API
🚧 Система уведомлений
🚧 Система отзывов

### Планируется
🔮 Система отзывов
🔮 Галерея фотографий
🔮 Чат поддержки
🔮 Админ-панель
🔮 Система управления бронированиями
🔮 Система уведомлений
🔮 Мультиязычность

## Особенности реализации

### Шрифты
Шрифты подключены через next/font/google с использованием CSS-переменных для оптимизации производительности.

### Мобильное меню
Реализовано с помощью состояния React и CSS-трансформаций для плавного появления/скрытия.

### Анимации
Используется react-transition-group для плавных переходов и hover-эффектов.

### Слайдер изображений в RoomCard
Реализован с помощью CSSTransition и SwitchTransition для плавной смены изображений при наведении мыши. Автоматическая смена каждые 5 секунд.

### Анимации переходов
Используются CSS-классы fade-enter, fade-enter-active, fade-exit, fade-exit-active для плавных переходов между слайдами.

### Авторизация
Реализована система входа по номеру телефона с подтверждением SMS-кодом и возможностью авторизации через социальные сети.

### Данные
Статические данные о номерах хранятся в data/rooms.ts в виде массива объектов с типизацией TypeScript.

### Конфигурация Tailwind CSS
- Используется классический синтаксис Tailwind v3 (@tailwind directives)
- Настроены кастомные шрифты через CSS-переменные
- Правильно сконфигурирован PostCSS с плагинами tailwindcss и autoprefixer

### Конфигурация шрифтов
Шрифты подключены через next/font/google с использованием CSS-переменных:
- Inter как основной шрифт (--font-inter)
- Karantina для логотипа и заголовков (--font-karantina)
- Istok Web для подзаголовков (--font-istok-web)
- Source Serif 4 для акцентного текста (--font-source-serif-pro)

## Команды разработки

```bash
# Установка зависимостей
npm install

# Установка с игнорированием конфликтов зависимостей
npm install --legacy-peer-deps

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск собранного приложения
npm run start

# Линтинг кода
npm run lint

# Очистка кэша Next.js
npx next clean
```

## Деплой

Приложение готово к деплою на Vercel через интеграцию с GitHub. Автоматическая сборка и деплой при пуше в main ветку.

## Контакты и поддержка

Для вопросов по проекту обращаться к владельцу репозитория.

## Подробное описание файлов проекта

### Корневые файлы
- **package.json** - файл конфигурации проекта, содержащий зависимости (next 14.2.3, react 18.3.1, react-dom 18.3.1, next-auth 4.24.13, @heroicons/react 2.2.0, date-fns 4.1.0, react-day-picker 9.13.0, react-transition-group 4.4.5), скрипты запуска (dev, build, start, lint) и метаданные проекта
- **package-lock.json** - файл блокировки зависимостей, фиксирующий точные версии установленных пакетов
- **next.config.js** - конфигурационный файл Next.js с настройками strictMode, обработчиками изображений (remotePatterns для images.unsplash.com) и параметрами сборки
- **tsconfig.json** - конфигурация TypeScript с настройками компиляции, путями и параметрами типизации
- **tailwind.config.ts** - конфигурационный файл Tailwind CSS с определением путей к файлам (app/**/*, components/**/*), расширенной темой (шрифты: inter, karantina, istok-web, source-serif-pro), цветами и плагинами
- **postcss.config.js** - конфигурация PostCSS с подключением плагинов tailwindcss и autoprefixer
- **eslint.config.mjs** - конфигурация ESLint для проверки качества кода
- **globals.css** - глобальные стили приложения с подключением Tailwind и настройками шрифтов
- **.env.local** - файл с локальными переменными окружения (SECRET, NEXTAUTH_URL, DATABASE_URL)
- **.gitignore** - список файлов и директорий, игнорируемых Git
- **README.md** - файл с описанием проекта, инструкциями по установке и запуску
- **types.ts** - файл с глобальными типами TypeScript для всего приложения: интерфейс Room (id, название, описание, изображения, удобства, цена, политики отеля), Profile (имя, email, телефон), Booking (id, номер, даты заезда/выезда, статус), DayData (дата, цена, доступность, скидка, событие)

### Директория app/ (страницы приложения)
- **app/layout.tsx** - корневой компонент макета приложения, настраивает шрифты (Inter, Karantina, Istok_Web, Source_Serif_4) с CSS-переменными, задает метаданные (title, description) и оборачивает children в NextAuthProvider
- **app/page.tsx** - главная страница приложения, экспортирует MainScreen
- **app/globals.css** - глобальные стили уровня приложения
- **app/favicon.ico** - файл иконки сайта
- **app/api/auth/[...nextauth]/route.ts** - маршрут аутентификации NextAuth.js с провайдерами Google и кастомным SMS провайдером, стратегией сессии JWT и кастомной страницей входа
- **app/login/page.tsx** - страница входа в систему с компонентом LoginScreen, оберткой для центрирования формы
- **app/profile/page.tsx** - страница профиля пользователя с вкладками (Текущее бронирование, История, Профиль, Уведомления, Поддержка), использует данные из '@/lib/data' и компоненты профиля
- **app/rooms/page.tsx** - страница каталога номеров отеля, отображает список номеров в виде сетки с использованием RoomCard
- **app/rooms/[id]/page.tsx** - динамическая страница деталей конкретного номера, отображает галерею, описание, политики и виджет бронирования
- **app/calendar/page.tsx** - страница календаря для выбора дат бронирования, использует компонент SmartCalendar
- **app/booking/page.tsx** - страница процесса бронирования, клиентский компонент с оберткой Suspense, получающий параметры (roomId, checkIn, checkOut) из searchParams и передающий их в BookingForm
- **app/booking/success/page.tsx** - страница подтверждения успешного бронирования с деталями поездки (номер бронирования, даты, стоимость), клиентский компонент с оберткой Suspense, получающий bookingId из searchParams и отображающий данные бронирования

### Директория components/ (компоненты)
- **components/MainScreen.tsx** - главный экран приложения, объединяющий Header, HeroSection и InfoSection
- **components/LoginScreen.tsx** - компонент экрана авторизации с двумя шагами (ввод телефона и подтверждение по SMS-коду), включает провайдеры входа (Google, Telegram, VK) и интеграцию с NextAuth
- **components/RoomCard.tsx** - карточка номера отеля с автослайдером изображений (переключается при наведении), описанием, удобствами, ценой и кнопками "Подробнее" и "Выбрать"
- **components/BookingForm.tsx** - форма для заполнения данных при бронировании, включающая поля для данных гостя (имя, телефон, email), выбора дополнительных услуг (завтрак, трансфер), виджет оплаты и расчета итоговой стоимости бронирования
- **components/layout/Header.tsx** - шапка сайта с навигацией и мобильным меню
- **components/layout/HeroSection.tsx** - герой-секция с баннером и виджетом поиска
- **components/layout/InfoSection.tsx** - информационная секция с карточками преимуществ отеля
- **components/ui/Icons.tsx** - набор SVG иконок для всего приложения
- **components/ui/BookingWidget.tsx** - виджет бронирования с выбором дат заезда/выезда, расчетом стоимости, принимающий цену за ночь и ID комнаты, использует useRouter для навигации на страницу бронирования с параметрами
- **components/ui/HeroSearchWidget.tsx** - виджет поиска в герой-секции
- **components/ui/ImageGallery.tsx** - галерея изображений для просмотра фото номеров
- **components/ui/InfoCard.tsx** - карточка информации с иконкой и текстом
- **components/ui/RoomDetails.tsx** - компонент с деталями номера
- **components/ui/RoomPolicies.tsx** - компонент с правилами и политиками номера
- **components/ui/SearchButton.tsx** - кнопка поиска
- **components/ui/SearchPill.tsx** - элемент интерфейса в виде таблетки для поиска
- **components/profile/CurrentBookingCard.tsx** - карточка текущего бронирования в профиле
- **components/profile/HistoryBookingCard.tsx** - карточка истории бронирования в профиле
- **components/profile/ProfileSettings.tsx** - настройки профиля пользователя с возможностью редактирования имени, email и телефона
- **components/calendar/BookingDetailsPanel.tsx** - панель деталей бронирования в календаре, отображающая информацию о выбранном диапазоне дат, стоимости и доступности
- **components/calendar/CalendarDayContent.tsx** - компонент содержимого дня в календаре, используется для кастомного отображения ячеек календаря с учетом цен и доступности
- **components/calendar/SmartCalendar.tsx** - умный календарь с выбором диапазона дат, визуализацией забронированных дат, адаптивным отображением (1 месяц на мобильных, 2 на десктопе) и интеграцией с панелью деталей

### Директория data/ (данные)
- **data/rooms.ts** - файл с интерфейсом Room и данными о номерах отеля (id, название, описание, изображения, удобства, цена, политики отеля), включая номера "Делюкс", "Стандартный" и "Семейный люкс"

### Директория lib/ (библиотека)
- **lib/data.ts** - вспомогательные асинхронные функции для работы с данными, включая функции получения всех номеров (getAllRooms), номера по ID (getRoomById), данных бронирования (getBookingById), профиля пользователя (getProfileData), текущих и прошлых бронирований (getCurrentBookings, getPastBookings), а также данных для календаря (getCalendarDataForMonth)

### Директория providers/ (провайдеры)
- **providers/NextAuthProvider.tsx** - провайдер аутентификации NextAuth.js для всего приложения, оборачивающий children в SessionProvider

### Директория project-context/ (контекст проекта)
- **project-context/context.md** - данный файл, содержащий контекст проекта
- **project-context/rules.md** - файл с правилами разработки проекта

### Директория public/ (публичные файлы)
- **public/** - публичные файлы (hero1.png, hero2.jpg, hero3.jpg, hero4.jpg - изображения для героя-секции, папка images/ с изображениями номеров deluxe-1.avif, deluxe-2.avif, иконки и вспомогательные файлы)
- **package.json** - файл конфигурации проекта, содержащий зависимости (next 14.2.3, react 18.3.1, react-dom 18.3.1, next-auth 4.24.13, @heroicons/react 2.2.0, date-fns 4.1.0, react-day-picker 9.13.0, react-transition-group 4.4.5), скрипты запуска (dev, build, start, lint) и метаданные проекта
- **package-lock.json** - файл блокировки зависимостей, фиксирующий точные версии установленных пакетов
- **next.config.js** - конфигурационный файл Next.js с настройками strictMode, обработчиками изображений (remotePatterns для images.unsplash.com) и параметрами сборки
- **tsconfig.json** - конфигурация TypeScript с настройками компиляции, путями и параметрами типизации
- **tailwind.config.ts** - конфигурационный файл Tailwind CSS с определением путей к файлам (app/**/*, components/**/*), расширенной темой (шрифты: inter, karantina, istok-web, source-serif-pro), цветами и плагинами
- **postcss.config.js** - конфигурация PostCSS с подключением плагинов tailwindcss и autoprefixer
- **eslint.config.mjs** - конфигурация ESLint для проверки качества кода
- **globals.css** - глобальные стили приложения с подключением Tailwind и настройками шрифтов
- **.env.local** - файл с локальными переменными окружения (SECRET, NEXTAUTH_URL, DATABASE_URL)
- **.gitignore** - список файлов и директорий, игнорируемых Git
- **README.md** - файл с описанием проекта, инструкциями по установке и запуску
- **types.ts** - файл с глобальными типами TypeScript для всего приложения: интерфейс Room (id, название, описание, изображения, удобства, цена, политики отеля), Profile (имя, email, телефон), Booking (id, номер, даты заезда/выезда, статус), DayData (дата, цена, доступность, скидка, событие)

### Директория app/ (страницы приложения)
- **app/layout.tsx** - корневой компонент макета приложения, настраивает шрифты (Inter, Karantina, Istok_Web, Source_Serif_4) с CSS-переменными, задает метаданные (title, description) и оборачивает children в NextAuthProvider
- **app/page.tsx** - главная страница приложения, экспортирует MainScreen
- **app/globals.css** - глобальные стили уровня приложения
- **app/favicon.ico** - файл иконки сайта
- **app/api/auth/[...nextauth]/route.ts** - маршрут аутентификации NextAuth.js с провайдерами Google и кастомным SMS провайдером, стратегией сессии JWT и кастомной страницей входа
- **app/login/page.tsx** - страница входа в систему с компонентом LoginScreen, оберткой для центрирования формы
- **app/profile/page.tsx** - страница профиля пользователя с вкладками (Текущее бронирование, История, Профиль, Уведомления, Поддержка), использует данные из '@/lib/data' и компоненты профиля
- **app/rooms/page.tsx** - страница каталога номеров отеля, отображает список номеров в виде сетки с использованием RoomCard
- **app/rooms/[id]/page.tsx** - динамическая страница деталей конкретного номера, отображает галерею, описание, политики и виджет бронирования
- **app/calendar/page.tsx** - страница календаря для выбора дат бронирования, использует компонент SmartCalendar
- **app/booking/page.tsx** - страница процесса бронирования, клиентский компонент с оберткой Suspense, получающий параметры (roomId, checkIn, checkOut) из searchParams и передающий их в BookingForm
- **app/booking/success/page.tsx** - страница подтверждения успешного бронирования с деталями поездки (номер бронирования, даты, стоимость), клиентский компонент с оберткой Suspense, получающий bookingId из searchParams и отображающий данные бронирования

### Директория components/ (компоненты)
- **components/MainScreen.tsx** - главный экран приложения, объединяющий Header, HeroSection и InfoSection
- **components/LoginScreen.tsx** - компонент экрана авторизации с двумя шагами (ввод телефона и подтверждение по SMS-коду), включает провайдеры входа (Google, Telegram, VK) и интеграцию с NextAuth
- **components/RoomCard.tsx** - карточка номера отеля с автослайдером изображений (переключается при наведении), описанием, удобствами, ценой и кнопками "Подробнее" и "Выбрать"
- **components/BookingForm.tsx** - форма для заполнения данных при бронировании, включающая поля для данных гостя (имя, телефон, email), выбора дополнительных услуг (завтрак, трансфер), виджет оплаты и расчета итоговой стоимости бронирования
- **components/layout/Header.tsx** - шапка сайта с навигацией и мобильным меню
- **components/layout/HeroSection.tsx** - герой-секция с баннером и виджетом поиска
- **components/layout/InfoSection.tsx** - информационная секция с карточками преимуществ отеля
- **components/ui/Icons.tsx** - набор SVG иконок для всего приложения
- **components/ui/BookingWidget.tsx** - виджет бронирования с выбором дат заезда/выезда, расчетом стоимости, принимающий цену за ночь и ID комнаты, использует useRouter для навигации на страницу бронирования с параметрами
- **components/ui/HeroSearchWidget.tsx** - виджет поиска в герой-секции
- **components/ui/ImageGallery.tsx** - галерея изображений для просмотра фото номеров
- **components/ui/InfoCard.tsx** - карточка информации с иконкой и текстом
- **components/ui/RoomDetails.tsx** - компонент с деталями номера
- **components/ui/RoomPolicies.tsx** - компонент с правилами и политиками номера
- **components/ui/SearchButton.tsx** - кнопка поиска
- **components/ui/SearchPill.tsx** - элемент интерфейса в виде таблетки для поиска
- **components/profile/CurrentBookingCard.tsx** - карточка текущего бронирования в профиле
- **components/profile/HistoryBookingCard.tsx** - карточка истории бронирования в профиле
- **components/profile/ProfileSettings.tsx** - настройки профиля пользователя
- **components/calendar/BookingDetailsPanel.tsx** - панель деталей бронирования в календаре, отображающая информацию о выбранном диапазоне дат, стоимости и доступности
- **components/calendar/CalendarDayContent.tsx** - компонент содержимого дня в календаре, используется для кастомного отображения ячеек календаря с учетом цен и доступности
- **components/calendar/SmartCalendar.tsx** - умный календарь с выбором диапазона дат, визуализацией забронированных дат, адаптивным отображением (1 месяц на мобильных, 2 на десктопе) и интеграцией с панелью деталей. Еще не сделан, самая проблемная зона. Нужно делать с нуля. 


### Директория data/ (данные)
- **data/rooms.ts** - файл с интерфейсом Room и данными о номерах отеля (id, название, описание, изображения, удобства, цена, политики отеля), включая номера "Делюкс", "Стандартный" и "Семейный люкс"

### Директория lib/ (библиотека)
- **lib/data.ts** - вспомогательные асинхронные функции для работы с данными, включая функции получения всех номеров (getAllRooms), номера по ID (getRoomById), данных бронирования (getBookingById), профиля пользователя (getProfileData), текущих и прошлых бронирований (getCurrentBookings, getPastBookings), а также данных для календаря (getCalendarDataForMonth)

### Директория providers/ (провайдеры)
- **providers/NextAuthProvider.tsx** - провайдер аутентификации NextAuth.js для всего приложения, оборачивающий children в SessionProvider

### Директория project-context/ (контекст проекта)
- **project-context/context.md** - данный файл, содержащий контекст проекта
- **project-context/rules.md** - файл с правилами разработки проекта

### Директория public/ (публичные файлы)
- **public/** - публичные файлы (изображения героев, иконки и т.д.)